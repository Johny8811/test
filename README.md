This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## About developer
- I never worked with Tailwind before
- I was using Next.js for a while in 2022

## Not completed
- quick filters by my choice (left panel)
- pagination — there is just a UI
- animations
- my idea: zoom in the profile image when the image is focused and the user taps the space bar 

## Highlights
- mainly I focused on clean, typed, well-structured code that is ready for next development
- I implemented debouncing to the "search input" to optimize api calls
